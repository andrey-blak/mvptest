buildscript {
    repositories {
        mavenCentral ()
    }
    dependencies {
        classpath group: 'com.android.tools.build', name: 'gradle', version: '1.3.0'
    }
}

task clean (type: Delete) {
    delete rootProject.buildDir
}
